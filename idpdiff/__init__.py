"""Torsion-space wrapped-diffusion model for IDP conformational ensembles.

Submodules are imported lazily; import what you need directly, e.g.
``from idpdiff.validation.reconstruct import reconstruct_backbone`` (numpy-only)
or ``from idpdiff.diffusion import WrappedDiffusion`` (requires torch).
"""
__version__ = "0.1.0"
